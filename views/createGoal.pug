extends layout

block content
  h1 Create a New Fitness Goal

  form(action="/goals/create", method="POST")
    label(for="title") Goal Title:
    input(type="text", name="title", required)
    button(type="submit") ✅ Create Goal

  a(href="/goals") 🔙 Back to Goal List
